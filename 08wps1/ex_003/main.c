#include <stdio.h>
#include <stdint.h>
#define TM 4

int percorrer_matriz(int*p, int m);

int main() {
	int estacoes[TM] = { 22,33,44,55 };
	
	return 0;
}

int percorrer_matriz(int*p, int m) {
	for (int *p = ; p != (estacoes + TM); ++p) {
		//printf(" %d, %p\n", (estacoes, TM, percorrer_matriz));
	}
	return g(*p, m);
}